# Elevate-Labs-AI-ML-Internship-Task-4
End-to-end pipeline for classifying breast tumors using logistic regression. Includes data scaling, model training, evaluation (confusion matrix, precision, recall, ROC-AUC), threshold tuning, coefficient interpretation, and a saved model (logistic_regression_model.pkl).
